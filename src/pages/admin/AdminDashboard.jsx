import React from 'react';
import TransitOperationsCockpit from '../../components/cockpit/TransitOperationsCockpit';

export default function AdminDashboard(props) {
  return <TransitOperationsCockpit {...props} />;
}
